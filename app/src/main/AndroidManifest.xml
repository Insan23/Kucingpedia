<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.insan.kucingpedia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Koleksiku"
            android:label="@string/title_activity_koleksiku"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".HalamanTambah" />
        <activity android:name=".HalamanAbout" />
        <activity android:name=".HalamanDetail" />
        <activity
            android:name=".HalamanUtama"
            android:label="@string/title_activity_halaman_utama"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HalamanMulai"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>